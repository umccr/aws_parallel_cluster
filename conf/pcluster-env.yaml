name: pcluster
channels:
  - anaconda
  - conda-forge
dependencies:
  - pandas
  - jq
  - python=3.8
  - pip
  - pip:
    - aws-parallelcluster == __AWS_PARALLEL_CLUSTER_VERSION__
    - aws2-wrap == 1.1.1
    - yawsso == 0.6.1
    - packaging == 20.4
