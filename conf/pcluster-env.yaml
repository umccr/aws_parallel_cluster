name: pcluster
channels:
  - anaconda
  - conda-forge
  - pandas
dependencies:
  - jq
  - python=3.8
  - pip
  - pip:
    - aws-parallelcluster == __AWS_PARALLEL_CLUSTER_VERSION__
    - aws2-wrap == 1.1.1
    - yawsso
    - packaging == 20.4
